---
name: sikdae-expense-report
description: 영수증 이미지/PDF/PPTX를 직접 판독해 일 한도를 적용한 개인경비(식대) 정산 대장 엑셀을 만든다. "식대 정리해줘", "영수증 엑셀로 뽑아줘", "이번 달 식대 정산" 같은 요청에 사용한다.
---

# 식대 정산 대장 생성 Skill

영수증을 폴더째로 받아서 **당신(Claude)이 직접 눈으로 읽어** 사용일자/상호/금액을 판단하고,
일 한도를 적용한 정산 대장 엑셀을 만든다. OCR 라이브러리도, 정규식 파싱도, 오타 보정 사전도 없다 —
판독 기준은 아래 "판독 규칙"뿐이다.

Python 이 담당하는 것은 **한도 판정과 엑셀 생성**뿐이다. 판독은 전적으로 당신 몫이다.

모든 명령은 이 SKILL.md 가 있는 스킬 디렉터리를 기준으로 실행한다 (`scripts/...` 상대경로).

---

## ⚠ 한도 우선순위 — 가장 중요한 규칙

일 한도는 **절대 코드에 하드코딩되어 있지 않다.** 아래 우선순위로 결정된다.

| 순위 | 출처 | 지정 방법 |
|---|---|---|
| 1 | **대화 지시** | 사용자가 대화로 말한 한도 → `--daily-limit-from-chat` |
| 2 | 실행 인자 | 사용자가 명령줄로 넘긴 한도 → `--daily-limit` |
| 3 | config 날짜 구간 | `config.json` 의 `daily_limit.history` (영수증 사용일자 기준) |
| 4 | config 기본값 | `config.json` 의 `daily_limit.default` |
| 5 | 내장 기본값 | config.json 이 없을 때만 (15,000원) |

**사용자가 대화 중에 "이번엔 한도 2만원으로 해줘", "이번 달은 2만원까지야", "한도 좀 올려서
계산해줘 2만원" 같이 말하면, 반드시 `--daily-limit-from-chat 20000` 을 붙여 실행한다.**
`--daily-limit` 이 아니라 `--daily-limit-from-chat` 을 쓰는 이유는, 결과에 찍히는 출처가
"대화 지시"로 정확히 기록되어야 하기 때문이다. 둘 다 주면 `--daily-limit-from-chat` 이 이긴다.

대화 지시는 **그 회차에만** 적용된다. `config.json` 은 건드리지 않는다.
사용자가 "규정이 아예 바뀌었다 / 앞으로 계속 이 한도다"라고 하면 그때는 대화 지시가 아니라
`config.json` 의 `history` 에 구간을 추가하도록 안내한다 (아래 "한도 영구 변경" 참조).

**실행 결과에 출력되는 '적용 한도'와 '출처'는 반드시 사용자에게 그대로 전달한다.**
어떤 한도로 계산됐는지 사용자가 모르는 채로 정산서를 제출하게 두면 안 된다.

---

## 워크플로우

### 1. 폴더 경로 확보
사용자가 영수증 폴더 경로를 아직 안 줬으면 물어본다.

### 2. 제출자 인적사항 확인
```
uv run python scripts/manage_config.py show
```
- 설정이 있으면 `{"user_name": ..., "position": ..., "department": ...}` JSON 한 줄이 출력된다.
- `NOT_FOUND` 가 나오면 대화로 성명/직책/부서를 물어보고 저장한다:
  ```
  uv run python scripts/manage_config.py save --name "홍길동" --position "매니저" --department "경영관리팀"
  ```

현재 한도 정책이 궁금하면 (사용자가 "지금 한도 얼마야?" 라고 물으면):
```
uv run python scripts/manage_config.py show-limit --on 2026-07-15
```

### 3. 영수증 스캔
```
uv run python scripts/prepare_receipts.py "<폴더경로>" --output "<임시경로>/manifest.json"
```
`N건 발견, temp_dir=...` 한 줄과 매니페스트 경로가 출력된다. `0건`이면 폴더 경로와
지원 형식(JPG/PNG/BMP/TIFF/PDF/PPTX)을 사용자에게 확인한다.

매니페스트 각 항목의 키:
- `image_path` — 비전으로 볼(그리고 엑셀에 삽입될) 이미지 경로. 변환 실패 시 `null`
- `source_file` — 원본 파일 경로
- `embedded_text` — PDF 에서 직접 추출된 텍스트 (없으면 `null`)
- `error` — 처리 실패 시에만 존재하는 키

### 4. 실패 항목 분리
`error` 키가 있거나 `image_path` 가 `null` 인 항목은 판독을 시도하지 말고 따로 모아둔다.
나중에 "처리 실패" 목록으로 보고한다.

### 5. 각 항목 판독
정상 변환된 항목의 `image_path` 를 직접 열어서 보고, `embedded_text` 가 있으면 참고 자료로
활용해 아래 판독 규칙에 따라 date / merchant / amount 를 정한다.

### 6. 사용자 확인
판독 결과를 표로 요약해서 보여준다. 상호나 금액이 이상해 보이는 항목은 확정 전에 물어본다.
**날짜나 금액을 못 읽었으면 짐작해서 채우지 말고 `null` 로 둔다** — 아래 7단계 참조.

### 7. 결과 JSON 작성
사용자가 확정한 목록만 (4단계에서 걸러낸 실패 항목은 제외) 다음 형식으로 저장한다:
```json
[
  {"date": "2026-07-02", "merchant": "행복분식", "amount": 18000, "image_path": "..."},
  {"date": null,         "merchant": "",         "amount": null,  "image_path": "..."}
]
```

**`date` 와 `amount` 를 읽지 못했으면 반드시 `null` 로 둔다.** 오늘 날짜나 0 이나 그럴듯한
숫자로 채우면 안 된다. `null` 로 두면 프로그램이 '확인 필요'로 분류해 붉은색으로 표시하고
합계에서 제외하지만, 값을 지어내면 사용자가 틀린 정산서를 그대로 제출하게 된다.
(상호는 못 읽어도 `""` 로 두면 된다 — 한도 계산에는 영향이 없다.)

### 8. 엑셀 생성
```
uv run python scripts/generate_excel.py "<결과.json>" --output "<원하는 경로>"
```
대화로 한도 지시를 받았다면:
```
uv run python scripts/generate_excel.py "<결과.json>" --daily-limit-from-chat 20000
```
`--output` 을 생략하면 `~/Downloads/{연월}_개인경비_사용내역서_{성명}.xlsx` 로 저장된다.

### 9. 결과 보고
사용자에게 반드시 함께 전달한다:
1. **적용된 한도와 출처** (실행 결과 상단 박스에 출력됨)
2. 인정금액 합계 (= 실제 청구액) / 초과금액 합계
3. **확인 필요 건 목록** — 엑셀에서 붉은색 행이며, 사용자가 직접 채워야 한다
4. 4단계에서 모아둔 처리 실패 목록 (있다면) — 엑셀에 수동 추가 필요
5. 저장 경로

---

## 판독 규칙

- **금액**: "합계" > "총합계" > "결제금액" > "거래금액" > "금액" 순으로 우선한다.
  부가세가 별도 표기돼 있으면 **부가세 포함 합계**를 쓴다 (공급가액만 가져오면 안 된다).
- **날짜**: `YYYY.MM.DD`, `YY.MM.DD`, `YYYY-MM-DD`, `2026년 7월 2일` 등 다양한 표기를 인식한다.
  출력은 항상 `YYYY-MM-DD` 로 정규화한다. 정말 못 읽겠으면 **추측하지 말고 `null`**.
- **상호명**: "가맹점명" 라벨 뒤의 실제 상호를 쓴다. "가맹점번호"는 번호 라벨이지 상호가 아니다.
  네이버페이/카카오페이 등 간편결제 화면에서는 **광고 배너 문구를 상호로 오인하지 말고**
  "결제 상세정보" 섹션의 실제 가맹점명만 사용한다.
- **다크모드**: 어두운 배경 + 밝은 글씨 영수증도 그대로 읽는다.
- **취소/환불 전표**: 승인취소·환불 표기가 있으면 정산 대상이 아니다. 사용자에게 알리고 제외한다.
- **비용 분담**: 동료와 나눠 낸 경우에도 전표에 찍힌 총 결제금액 하나만 기록한다
  (분담 계산은 하지 않는다 — 사용자가 엑셀에서 직접 수정한다).
- **애매하면 묻는다**: 위 규칙을 다 적용해도 확신이 안 서면 짐작하지 말고 사용자에게 물어본다.

---

## 한도 영구 변경 (규정이 실제로 바뀐 경우)

`config.json` 의 `daily_limit.history` 배열 **끝에 항목을 추가**한다. 기존 항목은 지우지 않는다.

```json
{
  "daily_limit": {
    "default": 15000,
    "history": [
      { "from": "2025-01-01", "amount": 12000, "note": "2025년 규정" },
      { "from": "2026-06-01", "amount": 15000, "note": "2026-06 인상" },
      { "from": "2026-07-01", "amount": 20000, "note": "2026-07 재인상" }
    ]
  }
}
```

영수증의 **사용일자**가 속한 구간의 한도가 적용되므로, 규정이 바뀌어도 **과거분이 소급
재계산되지 않는다.** 기존 항목을 지우면 그 기간의 과거 영수증이 잘못된 한도로 계산된다.

---

## 판정 로직 (Python 담당)

- 같은 날짜의 영수증은 **합산한 뒤** 그 날짜의 한도까지만 인정한다.
- 한도를 넘는 금액은 버리지 않고 **초과금액 컬럼**으로 분리한다. 원 결제금액은 항상 보존된다.
- 미사용 한도는 다음 날로 **이월되지 않는다.**
- 날짜나 금액이 `null` 인 건은 **한도 계산에서 제외**하고 '확인 필요'로 분리한다.

---

## 사전 조건

- 로컬 파일시스템 접근이 켜져 있어야 한다 (폴더 스캔, 엑셀 저장).
- `uv` 가 설치되어 있어야 한다.
- 이 폴더의 `pyproject.toml` 로 `uv run` 이 최초 실행 시 의존성을 자동 설치한다.
  최초 1회는 인터넷 연결이 필요하고 시간이 걸린다 (두 번째부터는 빠르다).
