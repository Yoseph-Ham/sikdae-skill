# 식대 정산 대장 생성기 (Claude Skill)

영수증 사진을 폴더째로 던지면, **Claude 가 영수증을 직접 읽고** 일 한도를 적용한
개인경비 정산 대장 엑셀을 만들어 주는 Claude Skill 입니다.

매달 영수증 수십 장을 손으로 엑셀에 옮겨 적고, 같은 날 두 건이면 한도 계산을 다시 하고,
규정이 바뀌면 어느 달치부터 새 한도인지 헷갈리는 — 그 반복 작업을 없애려고 만들었습니다.

```
영수증 폴더 (JPG/PNG/PDF/PPTX)
        │
        ▼
  prepare_receipts.py      폴더 스캔, PDF/PPTX → 이미지 변환
        │
        ▼
   ★ Claude 직접 판독 ★     사용일자 / 상호 / 금액
        │                   (OCR 라이브러리 없음)
        ▼
   limit_policy.py         날짜 구간별 일 한도 결정 + 출처 추적
   expense_processor.py    일자별 합산 → 한도 적용 → 인정/초과 분리
        │
        ▼
   excel_writer.py         회사 양식 xlsx (내역 시트 + 영수증 이미지 시트)
```

## 왜 OCR 을 걷어냈나

이전 버전은 EasyOCR 기반 데스크톱 앱이었습니다. 잘 되는 영수증은 잘 됐지만:

| 문제 | OCR 버전 | 현재 (Claude 판독) |
|---|---|---|
| 다크모드 영수증 | 이미지 반전 전처리 필요, 그래도 인식률 낮음 | 그냥 읽음 |
| 간편결제 스크린샷 | 광고 배너를 상호로 오인 → 예외 패턴 사전 관리 | 문맥으로 구분 |
| 상호명 오타 | 보정 사전 28개를 손으로 유지보수 | 불필요 |
| 부가세 vs 합계 | 키워드 우선순위 규칙으로 분기 | 영수증 구조를 보고 판단 |
| 배포 크기 | ~773MB (PyTorch 포함) | 스크립트 몇 개 |

판독 규칙은 코드가 아니라 [`SKILL.md`](SKILL.md) 에 자연어로 적혀 있어서,
새로운 영수증 형식이 나와도 사전이나 정규식이 아니라 문장 한 줄을 고치면 됩니다.

반대로 **한도 판정과 엑셀 생성은 Python 에 그대로 남겼습니다.** 돈 계산은 매번 똑같은 결과가
나와야 하고, 테스트로 고정할 수 있어야 하기 때문입니다.

## 설치

```bash
git clone <이 레포 주소>
cd sikdae-skill
```

Claude Desktop / Claude Code 의 skills 디렉터리에 이 폴더를 두거나 심볼릭 링크하면 됩니다.
의존성은 [`uv`](https://docs.astral.sh/uv/) 가 최초 실행 시 자동 설치합니다 (별도 `pip install` 불필요).

**요구사항**: Python 3.11+, `uv`, 로컬 파일시스템 접근 권한

## 사용법

### Claude 와 대화로 (주 사용 방식)

```
나: 이번 달 식대 정리해줘. 영수증은 D:\영수증\2026-07 에 있어
```

Claude 가 폴더를 스캔하고, 영수증을 한 장씩 읽고, 판독 결과를 표로 보여주며 확인을 받은 뒤
엑셀을 만들어 줍니다. 애매한 영수증은 확정 전에 되물어봅니다.

```
나: 이번엔 한도 2만원으로 계산해줘
```
이렇게 말하면 그 회차에만 2만원이 적용되고, 결과에 출처가 `대화 지시`로 기록됩니다.

### 명령줄로

```bash
# 1. 폴더 스캔
uv run python scripts/prepare_receipts.py "D:/영수증/2026-07" --output manifest.json

# 2. (판독 결과 JSON 을 준비한 뒤) 엑셀 생성
uv run python scripts/generate_excel.py receipts.json --output 정산서.xlsx

# 이번 회차만 다른 한도 적용
uv run python scripts/generate_excel.py receipts.json --daily-limit 20000

# 특정 날짜에 적용될 한도 확인
uv run python scripts/manage_config.py show-limit --on 2026-07-15
```

## 한도 설정

일 한도는 **코드 어디에도 하드코딩되어 있지 않습니다.** 아래 우선순위로 결정됩니다.

| 순위 | 출처 | 지정 방법 | 적용 범위 |
|---|---|---|---|
| 1 | 대화 지시 | Claude 에게 "이번엔 2만원" | 그 회차만 |
| 2 | 실행 인자 | `--daily-limit 20000` | 그 회차만 |
| 3 | config 날짜 구간 | `config.json` 의 `history` | 영수증 사용일자별 |
| 4 | config 기본값 | `config.json` 의 `default` | 구간에 안 걸리는 날짜 |
| 5 | 내장 기본값 | (config.json 이 없을 때만) 15,000원 | 전체 |

### config.json — 날짜 구간 이력

규정이 바뀌어도 **과거분이 소급 재계산되지 않는 것**이 이 설계의 핵심입니다.

```json
{
  "daily_limit": {
    "default": 15000,
    "history": [
      { "from": "2025-01-01", "amount": 12000, "note": "2025년 규정" },
      { "from": "2026-06-01", "amount": 15000, "note": "2026-06 인상" },
      { "from": "2026-07-01", "amount": 20000, "note": "2026-07 재인상" }
    ]
  }
}
```

영수증의 **사용일자**가 속한 구간의 한도가 적용됩니다.
위 설정이면 `2026-05-28` 영수증은 12,000원, `2026-07-02` 영수증은 20,000원 기준으로 계산됩니다.
한 번의 실행에 두 구간이 섞여 있어도 각각 올바른 한도가 적용됩니다.

규정이 바뀌면 **배열 끝에 항목을 추가**하기만 하면 됩니다. 파일을 저장하는 즉시 반영되고,
코드를 고칠 필요가 없습니다. **기존 항목은 지우지 마세요** — 지우면 그 기간의 과거 영수증이
잘못된 한도로 계산됩니다.

> YAML 이 아니라 JSON 을 쓰는 이유: 외부 파서 의존성 없이 표준 라이브러리만으로 읽기 위해서입니다.
> 주석이 필요하면 `_comment` 필드를 쓰면 됩니다 (`samples/config.sample.json` 참고).

## 판정 규칙

- **같은 날 여러 건은 합산 후 한도까지만 인정**하고, 초과분은 `초과금액` 컬럼으로 분리합니다.
  원 결제금액은 절대 덮어쓰지 않습니다.
- **미사용 한도는 이월되지 않습니다.**
- **날짜나 금액을 읽지 못한 영수증은 임의로 채우지 않습니다.** '확인 필요'로 분류해
  붉은색으로 표시하고, 합계에서 제외합니다. 사람이 원본을 보고 직접 채워야 합니다.
  (금액 칸을 `0` 으로 채우면 "0원 결제"로 읽혀 오류를 놓치기 때문에 빈 칸으로 둡니다.)

## 예시 출력

### 터미널

```
========================================================
적용 한도: 사용일자 구간에 따라 다름
  - 2026-05-28 ~ 2026-05-28: 12,000원  (출처: config.json 구간 2025-01-01~ (2025년 규정))
  - 2026-07-02 ~ 2026-07-06: 20,000원  (출처: config.json 구간 2026-07-01~ (2026-07 재인상))
========================================================
영수증 7건 처리
  결제금액 합계 : 77,500원
  인정금액 합계 : 63,000원   ← 실제 청구액
  초과금액 합계 : 14,500원
  확인 필요     : 1건 (한도 계산에서 제외됨)
      - (상호 미판독): 확인 필요: 사용일자 미판독, 금액 미판독, 상호 미판독 — 원본 영수증을 보고 직접 입력
```

대화로 한도를 지시한 경우:

```
========================================================
적용 한도: 20,000원
출처: 대화 지시
(config.json 의 날짜 구간 이력보다 우선 적용되었습니다)
========================================================
```

### 엑셀 (Sheet 1: 개인경비 사용내역서)

| NO. | 사용날짜 | 사용처 | 항목 구분 | 사용내역 | 결제금액 | 인정금액 | 초과금액 | 참석자 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 26.05.28 | 옛날국밥 예시점 | 식대 | 점심식대 | 14,000 | 12,000 | 2,000 | 홍길동 | 한도 초과 2,000원 (일 한도 12,000원) |
| 2 | 26.07.02 | 행복분식 테스트점 | 식대 | 점심식대 | 18,000 | 18,000 | | 홍길동 | |
| 3 | 26.07.02 | 카페 하루 (가상) | 식대 | 점심식대 | 9,000 | 2,000 | 7,000 | 홍길동 | 한도 초과 7,000원 (일 한도 20,000원) |
| 4 | 26.07.03 | 미래식당 3호점 | 식대 | 점심식대 | 11,000 | 11,000 | | 홍길동 | |
| 5 | 26.07.06 | 면류연구소 (샘플) | 식대 | 점심식대 | 13,500 | 13,500 | | 홍길동 | |
| 6 | 26.07.06 | 그린샐러드 데모점 | 식대 | 점심식대 | 12,000 | 6,500 | 5,500 | 홍길동 | 한도 초과 5,500원 (일 한도 20,000원) |
| 7 | | (상호 미판독) | 식대 | 점심식대 | | | | 홍길동 | 🔴 확인 필요: 사용일자 미판독, 금액 미판독 |
| | **합계** | | | | **77,500** | **63,000** | **14,500** | ← 인정금액이 실제 청구액입니다 | |

- 2행·3행이 같은 날(07-02)이라 합산 27,000원 → 한도 20,000원까지만 인정, 7,000원은 초과분
- 1행은 규정 변경 **이전** 날짜라 12,000원 한도가 적용됨 (소급 재계산 없음)
- 7행은 붉은색 — 판독 실패 건이며 합계에 포함되지 않음
- 같은 날 복수 결제는 색상으로 그룹 표시됨
- 시트 하단에 **적용된 한도와 출처가 기록**되어, 나중에 파일만 봐도 계산 근거를 알 수 있음

**Sheet 2 (영수증)**: 원본 영수증 이미지가 5열 그리드로 첨부됩니다.

카드사에서 받는 PDF 전표는 A4 한가운데에 영수증이 작게 박혀 있어 그대로 넣으면
셀의 대부분이 흰 여백입니다. 변환 단계에서 여백을 자동으로 잘라내
(실측: 면적 100% → 26%) 영수증이 알아볼 수 있는 크기로 들어갑니다.
어두운 배경의 다크모드 영수증도 동일하게 처리됩니다.

## 데모 돌려보기

이 레포에는 **실제 영수증이 단 한 장도 들어있지 않습니다.** 데모용 가상 영수증을 직접 생성합니다.

```bash
# 가상 영수증 이미지 7장 생성 (전부 지어낸 상호/금액)
uv run python samples/make_sample_receipts.py --output samples/receipts

# 위 예시 출력 그대로 재현
uv run python scripts/generate_excel.py samples/receipts.sample.json \
    --config samples/config.sample.json \
    --name "홍길동" --position "매니저" --department "경영관리팀" \
    --output demo.xlsx
```

## 테스트

```bash
uv run python tests/run_all.py
```

82개 테스트 / 6개 스위트:

| 스위트 | 개수 | 검증 내용 |
|---|---|---|
| `test_models.py` | 6 | 판독 실패를 0/오늘날짜로 채우지 않는지 |
| `test_image_utils.py` | 11 | 여백 크롭이 내용을 자르지 않는지, 다크모드·얼룩 내성 |
| `test_limit_policy.py` | 18 | 날짜 구간 선택, 우선순위, 출처 추적, config 폴백 |
| `test_expense_processor.py` | 24 | 일자별 합산·한도, 초과분 분리, 확인필요 분류, 이월 금지 |
| `test_prepare_receipts.py` | 6 | 폴더 스캔, PDF/PPTX 변환, 손상 파일 격리 |
| `test_generate_excel_cli.py` | 17 | CLI 전 구간 + 생성된 xlsx 셀 값 직접 검증 |

## 프로젝트 구조

```
sikdae-skill/
├── SKILL.md                    Claude 가 따르는 워크플로우 · 판독 규칙 · 한도 우선순위
├── config.json                 일 한도 설정 (날짜 구간 이력)
├── scripts/
│   ├── limit_policy.py         한도 결정 + 출처 추적
│   ├── expense_processor.py    일자별 합산 · 한도 적용 · 인정/초과 분리
│   ├── excel_writer.py         회사 양식 xlsx 생성
│   ├── generate_excel.py       CLI 진입점
│   ├── prepare_receipts.py     폴더 스캔 → 매니페스트
│   ├── manage_config.py        인적사항 저장 / 한도 조회 CLI
│   ├── models.py               ReceiptData
│   ├── config_store.py         제출자 인적사항
│   ├── image_utils.py          영수증 여백 크롭
│   ├── pdf_utils.py            PDF → 텍스트 or 이미지
│   └── pptx_utils.py           PPTX → 이미지
├── tests/
└── samples/                    전부 가상 데이터
```

## 개인정보 취급

이 레포는 공개를 전제로 만들어졌습니다.

- **실제 영수증·정산서·직원 실명은 커밋되지 않습니다.** `.gitignore` 가 입력/출력 폴더 전체와
  모든 이미지·문서·엑셀 확장자를 차단합니다.
- 제출자 인적사항은 레포가 아니라 `~/.sikdae_config.json` 에 저장됩니다 (역시 gitignore 대상).
- `samples/` 의 상호·금액·카드번호·사업자번호·인명은 **전부 지어낸 것**이며 실존 대상과 무관합니다.

새 입력/출력 폴더를 추가할 때는 `.gitignore` 에 먼저 등록하세요.

### 배포할 때 주의

`.gitignore` 는 **커밋만** 막습니다. 폴더를 통째로 zip 으로 묶으면 무시 대상 파일까지
그대로 딸려갑니다 — 생성해 둔 실제 정산서가 여기 해당합니다.
배포용 zip 은 추적 중인 파일만 묶으세요:

```bash
git archive --format=zip -o sikdae-skill.zip HEAD
```

## 라이선스

MIT
